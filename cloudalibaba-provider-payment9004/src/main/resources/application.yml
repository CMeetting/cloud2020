server:
  port: 9004

spring:
  application:
    name: cloud-nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:3333,localhost:4444,localhost:5555

management:
  endpoints:
    web:
      exposure:
        include: '*'
